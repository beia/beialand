version: "2"
services:
  citisim_app:
    container_name: beia/citisim_app
    ports:
      - "32100:80"

  citisim_db:
    image: mysql:5.7
    ports:
      - "32000:3306"
    environment:
      MYSQL_ROOT_PASSWORD: toor
    volumes:
      - ./db:/docker-entrypoint-initdb.d/:ro
